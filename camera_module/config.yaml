camera:
  serial_number: "3200130"   # printed on the camera label
  ip: null                   # e.g. "192.168.1.100" — used if serial_number is null
  cti_file: null             # null = auto-detected from GENICAM_GENTL64_PATH

capture:
  delay: 0.5                 # seconds between captures
  output_dir: "C:/Users/KfirShuster/PycharmProjects/best_camera_module/captures"
  exposure_time: null        # microseconds (e.g. 20000 = 20ms) — null = camera default
  gain: null                 # dB (e.g. 6.0) — null = camera default
  pixel_format: "BayerRG8"  # BayerRG8 = colour | Mono8 = greyscale | RGB8 = colour on-camera
